# 🚀 快速部署 - 域名配置（aigctmp.wzbc.edu.cn）

## 📌 核心概念

**重要**: 服务器显示 `127.0.0.1` 是正常的！

- `127.0.0.1` = 本机地址（只能本机访问）
- `10.145.251.29` = 服务器IP（局域网可访问）
- `aigctmp.wzbc.edu.cn` = 域名（需要DNS配置）

---

## ⚡ 5分钟快速部署

### 第1步：构建项目（2分钟）

```bash
# 在项目目录执行
cd /path/to/app-7z58i603if41
npm run build
```

✅ 成功标志：生成 `dist/` 目录

### 第2步：部署文件（1分钟）

```bash
# 复制文件到Apache目录
sudo cp -r dist/* /var/www/aigctmp/

# 设置权限
sudo chown -R www-data:www-data /var/www/aigctmp
sudo chmod -R 755 /var/www/aigctmp
```

### 第3步：配置Apache（1分钟）

```bash
# 创建配置文件
sudo nano /etc/apache2/sites-available/aigctmp.conf
```

粘贴以下内容：

```apache
<VirtualHost *:80>
    ServerName aigctmp.wzbc.edu.cn
    DocumentRoot /var/www/aigctmp

    <Directory /var/www/aigctmp>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted

        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>
</VirtualHost>
```

保存并启用：

```bash
sudo a2ensite aigctmp.conf
sudo a2enmod rewrite
sudo systemctl restart apache2
```

### 第4步：配置DNS（需要网络管理员）

联系学校网络管理员，添加DNS记录：

```
类型: A
域名: aigctmp.wzbc.edu.cn
IP: 10.145.251.29
```

### 第5步：测试访问（1分钟）

```bash
# 测试DNS
ping aigctmp.wzbc.edu.cn

# 测试网站
curl -I http://aigctmp.wzbc.edu.cn
```

浏览器访问：`http://aigctmp.wzbc.edu.cn`

---

## 🔧 临时测试方法（DNS未配置时）

如果DNS还未配置，可以临时修改hosts文件测试：

### Windows系统

1. 用管理员权限打开记事本
2. 打开文件：`C:\Windows\System32\drivers\etc\hosts`
3. 添加一行：`10.145.251.29  aigctmp.wzbc.edu.cn`
4. 保存
5. 访问：`http://aigctmp.wzbc.edu.cn`

### Linux/Mac系统

```bash
# 编辑hosts文件
sudo nano /etc/hosts

# 添加一行
10.145.251.29  aigctmp.wzbc.edu.cn

# 保存后访问
http://aigctmp.wzbc.edu.cn
```

---

## ❌ 常见错误

### 错误1: 使用开发服务器（npm run dev）

**问题**: 
```bash
npm run dev
# 显示: http://127.0.0.1:5173
```

**为什么错误**:
- 开发服务器只用于本地开发
- 不适合生产环境
- 性能差，不稳定

**正确做法**:
```bash
npm run build  # 构建
# 然后部署到Apache
```

### 错误2: 直接访问IP+端口

**问题**: 
```
http://10.145.251.29:5173
```

**为什么错误**:
- 5173是开发端口
- 生产环境不应该暴露端口
- 不安全

**正确做法**:
```
http://aigctmp.wzbc.edu.cn  # 使用域名，默认80端口
```

### 错误3: 忘记配置DNS

**问题**: 域名无法访问

**检查**:
```bash
ping aigctmp.wzbc.edu.cn
# 如果失败，说明DNS未配置
```

**解决**: 联系网络管理员配置DNS

---

## 📋 部署检查清单

### 服务器端
- [ ] 项目已构建（`npm run build`）
- [ ] 文件在 `/var/www/aigctmp/`
- [ ] Apache配置已创建
- [ ] Apache已重启
- [ ] 可以通过IP访问（`http://10.145.251.29`）

### DNS配置
- [ ] DNS记录已添加
- [ ] DNS已生效（`ping aigctmp.wzbc.edu.cn` 成功）
- [ ] 可以通过域名访问（`http://aigctmp.wzbc.edu.cn`）

### 功能测试
- [ ] 首页正常显示
- [ ] 所有页面可访问
- [ ] 页面刷新不出现404
- [ ] CAS登录正常

---

## 🆘 快速故障排查

### 问题：域名无法访问

```bash
# 1. 检查DNS
ping aigctmp.wzbc.edu.cn
# 如果失败 → DNS未配置，联系网络管理员

# 2. 检查Apache
sudo systemctl status apache2
# 如果未运行 → sudo systemctl start apache2

# 3. 检查配置
ls -l /etc/apache2/sites-enabled/ | grep aigctmp
# 如果没有 → sudo a2ensite aigctmp.conf

# 4. 查看日志
sudo tail -f /var/log/apache2/error.log
```

### 问题：页面刷新后404

```bash
# 启用rewrite模块
sudo a2enmod rewrite
sudo systemctl restart apache2
```

### 问题：显示Apache默认页面

```bash
# 检查DocumentRoot
cat /etc/apache2/sites-enabled/aigctmp.conf | grep DocumentRoot
# 应该是: /var/www/aigctmp

# 检查文件
ls -la /var/www/aigctmp/
# 应该有: index.html, assets/, 等
```

---

## 💡 关键命令

```bash
# 构建项目
npm run build

# 部署文件
sudo cp -r dist/* /var/www/aigctmp/

# 重启Apache
sudo systemctl restart apache2

# 查看日志
sudo tail -f /var/log/apache2/aigctmp_error.log

# 测试DNS
ping aigctmp.wzbc.edu.cn

# 测试网站
curl -I http://aigctmp.wzbc.edu.cn
```

---

## 📞 需要帮助？

| 问题 | 查看文档 |
|------|---------|
| **详细配置步骤** | [域名配置指南.md](./域名配置指南.md) |
| **零基础部署** | [本地部署指南.md](./本地部署指南.md) |
| **HTTPS配置** | [域名配置指南.md](./域名配置指南.md) 第3节 |

---

## ✅ 成功标志

部署成功后，您应该能够：

1. ✅ 通过域名访问：`http://aigctmp.wzbc.edu.cn`
2. ✅ 看到精美的首页轮播图
3. ✅ 所有页面正常显示
4. ✅ 页面刷新不出现404
5. ✅ CAS登录正常工作

---

**提示**: 如果DNS配置需要时间生效（通常几分钟到几小时），可以先用hosts文件临时测试。

**文档版本**: 1.0  
**更新日期**: 2025-12-03  
**适用场景**: 快速部署和域名配置
