# 🚀 智能体比赛报名平台 - 本地部署指南（零基础版）

## 📖 项目介绍

这是一个**智能体比赛报名网站**，主要功能包括：

- 🏠 **首页展示** - 精美的轮播图和比赛信息展示
- 📝 **比赛规则** - 详细的参赛规则和评分标准
- 🔐 **CAS登录** - 使用温州商学院学号登录（单点登录）
- 📋 **在线报名** - 填写报名表单
- 📤 **作品提交** - 上传参赛作品
- 👤 **个人中心** - 查看报名状态

**部署方式**: 本地服务器部署（不使用云服务）

**访问地址**: https://aigctmp.wzbc.edu.cn

**服务器IP**: 10.145.251.29

---

## 🎯 部署前准备

### 需要的软件

1. **Node.js** (版本 18 或更高)
   - 下载地址: https://nodejs.org/
   - 选择"LTS"版本下载安装

2. **Apache Web服务器** (已安装在服务器上)
   - 用于托管网站文件

3. **SSL证书** (用于HTTPS)
   - 需要为域名 `aigctmp.wzbc.edu.cn` 申请证书

### 需要的信息

- ✅ 服务器IP: `10.145.251.29`
- ✅ 域名: `aigctmp.wzbc.edu.cn`
- ✅ CAS Client ID: `CijBwB5EwTTXouO7`
- ✅ CAS Client Secret: `O8dOsXE7p7yMbh18KEP2Z6`

---

## 📦 第一步：获取项目代码

### 方式1: 如果有Git

```bash
# 克隆项目
git clone <项目地址>
cd app-7z58i603if41
```

### 方式2: 如果没有Git

1. 下载项目压缩包
2. 解压到任意目录
3. 打开命令行，进入项目目录

---

## ⚙️ 第二步：安装Node.js

### Windows系统

1. 访问 https://nodejs.org/
2. 下载"LTS"版本（推荐版本）
3. 双击安装包，一路"下一步"
4. 安装完成后，打开命令提示符（CMD）
5. 输入以下命令验证安装：

```bash
node --version
# 应该显示: v18.x.x 或更高版本

npm --version
# 应该显示: 9.x.x 或更高版本
```

### Linux系统（服务器）

```bash
# 安装Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version
```

---

## 🔧 第三步：配置项目

### 1. 进入项目目录

```bash
# Windows
cd C:\path\to\app-7z58i603if41

# Linux
cd /path/to/app-7z58i603if41
```

### 2. 安装项目依赖

```bash
# 安装依赖（需要5-10分钟）
npm install
```

**如果安装很慢**，可以使用国内镜像：

```bash
# 设置淘宝镜像
npm config set registry https://registry.npmmirror.com

# 再次安装
npm install
```

### 3. 配置环境变量

```bash
# 复制配置文件
cp .env.example .env

# 编辑.env文件
# Windows: notepad .env
# Linux: nano .env
```

`.env` 文件内容（已经配置好，无需修改）：

```bash
# 本地部署配置
VITE_APP_URL=https://aigctmp.wzbc.edu.cn
VITE_APP_ID=aigc_wzbc
```

---

## 🏗️ 第四步：构建项目

```bash
# 构建生产版本
npm run build
```

**构建过程**:
- 时间: 约1-2分钟
- 输出: `dist/` 目录
- 内容: 网站的所有静态文件（HTML, CSS, JS, 图片等）

**构建成功的标志**:
```
✓ built in 30s
dist/index.html                   x.xx kB
dist/assets/index-xxxxx.js        xxx kB
...
```

---

## 🌐 第五步：部署到Apache服务器

### 1. 准备服务器目录

```bash
# 登录服务器（如果在本地，跳过此步）
ssh user@10.145.251.29

# 创建网站目录
sudo mkdir -p /var/www/aigctmp
```

### 2. 上传文件到服务器

**方式1: 使用SCP（推荐）**

```bash
# 在本地电脑上执行
scp -r dist/* user@10.145.251.29:/var/www/aigctmp/
```

**方式2: 使用FTP工具**

1. 下载FileZilla: https://filezilla-project.org/
2. 连接到服务器 `10.145.251.29`
3. 将 `dist/` 目录下的所有文件上传到 `/var/www/aigctmp/`

**方式3: 如果在服务器上直接构建**

```bash
# 直接复制
sudo cp -r dist/* /var/www/aigctmp/
```

### 3. 设置文件权限

```bash
# 设置所有者
sudo chown -R www-data:www-data /var/www/aigctmp

# 设置目录权限
sudo find /var/www/aigctmp -type d -exec chmod 755 {} \;

# 设置文件权限
sudo find /var/www/aigctmp -type f -exec chmod 644 {} \;
```

---

## 🔐 第六步：配置Apache和SSL

### 1. 创建Apache配置文件

```bash
# 创建配置文件
sudo nano /etc/apache2/sites-available/aigctmp.conf
```

### 2. 粘贴以下配置

```apache
<VirtualHost *:443>
    ServerName aigctmp.wzbc.edu.cn
    DocumentRoot /var/www/aigctmp

    # SSL配置
    SSLEngine on
    SSLCertificateFile /path/to/your/cert.crt
    SSLCertificateKeyFile /path/to/your/cert.key
    SSLCertificateChainFile /path/to/your/chain.crt

    # 安全头
    Header always set Strict-Transport-Security "max-age=31536000"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-Content-Type-Options "nosniff"

    # 网站目录配置
    <Directory /var/www/aigctmp>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted

        # SPA路由支持（重要！）
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>

    # 日志
    ErrorLog ${APACHE_LOG_DIR}/aigctmp_error.log
    CustomLog ${APACHE_LOG_DIR}/aigctmp_access.log combined
</VirtualHost>

# HTTP自动跳转到HTTPS
<VirtualHost *:80>
    ServerName aigctmp.wzbc.edu.cn
    Redirect permanent / https://aigctmp.wzbc.edu.cn/
</VirtualHost>
```

**重要**: 修改SSL证书路径：
- `/path/to/your/cert.crt` → 你的证书文件路径
- `/path/to/your/cert.key` → 你的私钥文件路径
- `/path/to/your/chain.crt` → 你的证书链文件路径

### 3. 启用配置

```bash
# 启用网站
sudo a2ensite aigctmp.conf

# 启用必要的Apache模块
sudo a2enmod ssl
sudo a2enmod rewrite
sudo a2enmod headers

# 测试配置
sudo apache2ctl configtest
# 应该显示: Syntax OK

# 重启Apache
sudo systemctl restart apache2
```

---

## 🎯 第七步：配置CAS回调地址

### 联系学校信息中心

需要在CAS系统中注册以下回调地址：

```
https://aigctmp.wzbc.edu.cn/auth/callback
```

**联系方式**: 学校信息中心

**需要提供的信息**:
- 网站域名: `aigctmp.wzbc.edu.cn`
- 回调地址: `https://aigctmp.wzbc.edu.cn/auth/callback`
- Client ID: `CijBwB5EwTTXouO7` (已有)

---

## ✅ 第八步：测试网站

### 1. 测试HTTPS访问

打开浏览器，访问：

```
https://aigctmp.wzbc.edu.cn
```

**应该看到**:
- ✅ 精美的首页轮播图
- ✅ 比赛信息展示
- ✅ 导航栏正常显示
- ✅ 浏览器地址栏显示🔒（安全锁）

### 2. 测试页面导航

点击导航栏的链接，测试以下页面：

- ✅ 首页 (/)
- ✅ 比赛规则 (/rules)
- ✅ 在线报名 (/register)
- ✅ 作品提交 (/submit)
- ✅ 我的报名 (/my-registration)

**所有页面都应该正常显示，不出现404错误**

### 3. 测试CAS登录

1. 点击"登录"按钮
2. 应该跳转到CAS登录页面: `https://cas.wzbc.edu.cn/...`
3. 输入学号和密码
4. 登录成功后，应该跳转回网站首页
5. Header应该显示用户信息

### 4. 测试注销

1. 点击"退出"按钮
2. 应该跳转到CAS注销页面
3. 注销成功后，重定向回首页
4. 用户信息应该消失

---

## 🔍 常见问题排查

### 问题1: 访问网站显示404

**原因**: Apache配置错误或文件未上传

**解决**:
```bash
# 检查文件是否存在
ls -la /var/www/aigctmp/

# 应该看到: index.html, assets/, 等文件

# 检查Apache配置
sudo apache2ctl configtest

# 查看错误日志
sudo tail -f /var/log/apache2/aigctmp_error.log
```

### 问题2: 页面刷新后显示404

**原因**: SPA路由配置未生效

**解决**:
```bash
# 确保rewrite模块已启用
sudo a2enmod rewrite

# 检查.htaccess文件
ls -la /var/www/aigctmp/.htaccess

# 如果不存在，创建它
sudo nano /var/www/aigctmp/.htaccess
```

`.htaccess` 内容：
```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

### 问题3: SSL证书错误

**原因**: 证书配置错误或证书过期

**解决**:
```bash
# 检查证书
openssl x509 -in /path/to/cert.crt -text -noout

# 检查证书有效期
openssl x509 -in /path/to/cert.crt -noout -dates
```

### 问题4: CAS登录后跳转失败

**原因**: 回调地址未在CAS系统注册

**解决**:
1. 联系学校信息中心
2. 确认回调地址已注册: `https://aigctmp.wzbc.edu.cn/auth/callback`
3. 确认使用HTTPS协议

### 问题5: 构建失败

**原因**: 依赖安装不完整或Node.js版本过低

**解决**:
```bash
# 删除依赖重新安装
rm -rf node_modules package-lock.json
npm install

# 检查Node.js版本
node --version
# 应该 >= 18.0.0

# 如果版本过低，升级Node.js
```

---

## 📝 日常维护

### 更新网站内容

```bash
# 1. 修改代码后，重新构建
npm run build

# 2. 上传到服务器
scp -r dist/* user@10.145.251.29:/var/www/aigctmp/

# 3. 清除浏览器缓存
# 访问网站，按 Ctrl+Shift+R 强制刷新
```

### 查看访问日志

```bash
# 访问日志
sudo tail -f /var/log/apache2/aigctmp_access.log

# 错误日志
sudo tail -f /var/log/apache2/aigctmp_error.log
```

### 重启Apache

```bash
# 重启Apache
sudo systemctl restart apache2

# 检查Apache状态
sudo systemctl status apache2
```

---

## 📞 需要帮助？

### 问题分类

| 问题类型 | 解决方法 |
|---------|---------|
| **Node.js安装** | 参考"第二步：安装Node.js" |
| **构建失败** | 参考"问题5: 构建失败" |
| **Apache配置** | 参考"第六步：配置Apache和SSL" |
| **SSL证书** | 联系服务器管理员 |
| **CAS登录** | 联系学校信息中心 |
| **404错误** | 参考"问题1"和"问题2" |

### 检查清单

部署前检查：
- [ ] Node.js已安装（版本 >= 18）
- [ ] 项目依赖已安装（npm install）
- [ ] 项目已构建（npm run build）
- [ ] dist/目录已生成

服务器检查：
- [ ] 文件已上传到 /var/www/aigctmp/
- [ ] 文件权限已设置
- [ ] Apache配置文件已创建
- [ ] SSL证书已配置
- [ ] Apache已重启

功能检查：
- [ ] HTTPS访问正常
- [ ] 所有页面可访问
- [ ] CAS登录正常
- [ ] CAS注销正常

---

## 🎉 完成！

恭喜！您已经成功部署了智能体比赛报名平台！

**访问地址**: https://aigctmp.wzbc.edu.cn

**下一步**:
1. 测试所有功能
2. 通知用户可以访问
3. 收集反馈并改进

---

## 📋 快速命令参考

```bash
# 安装依赖
npm install

# 本地开发
npm run dev
# 访问: http://localhost:5173

# 构建生产版本
npm run build

# 上传到服务器
scp -r dist/* user@10.145.251.29:/var/www/aigctmp/

# 重启Apache
sudo systemctl restart apache2

# 查看日志
sudo tail -f /var/log/apache2/aigctmp_error.log
```

---

**文档版本**: 1.0  
**更新日期**: 2025-12-03  
**适用对象**: 零基础用户  
**部署方式**: 本地服务器部署
