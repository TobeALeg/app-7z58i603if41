# 立即执行 - OAuth登录修复

## 🎯 问题现状

- ❌ Edge Function返回500错误
- ❌ 错误信息：`error=invalid_request`
- ❌ CAS服务器返回400

## ✅ 解决方案

已实现**OAuth请求智能重试机制**，自动尝试三种请求方式：
1. HTTP Basic Authentication（标准OAuth 2.0）
2. POST + 参数在请求体（常见实现）
3. GET + 参数在URL（CAS特殊实现）

## 📋 执行步骤

### 第1步：重新部署Edge Function

```bash
cd ~/projects/app-7z58i603if41
./redeploy-edge-function.sh
```

**预期输出**：
```
=========================================
  重新部署Edge Function
=========================================

1. 当前Edge Function列表:
...

2. 重新部署oauth-callback函数...
✓ Edge Function部署成功

3. 查看最新日志...
...
```

---

### 第2步：测试Edge Function

```bash
./check-supabase.sh
```

**预期结果**：

✅ **成功情况**：
```
HTTP状态码: 200
响应内容: {"success":true,...}
```

或者：
```
HTTP状态码: 400
响应内容: {"error":"invalid_grant"}
```
（这是正常的，因为test code无效）

❌ **仍然失败**：
```
HTTP状态码: 500
响应内容: {"error":"获取access_token失败"}
```
（如果看到这个，请查看Edge Function日志）

---

### 第3步：查看Edge Function日志

```bash
supabase functions logs oauth-callback --follow
```

**关键日志内容**：

✅ **成功的日志**：
```
收到授权码，开始换取access_token...
尝试方式1: Basic Auth
请求参数: grant_type=authorization_code&code=...
Token响应: { access_token: "...", ... }
成功获取access_token，开始获取用户信息...
```

或者：
```
尝试方式1: Basic Auth
Basic Auth失败，尝试方式2: 参数在请求体
Token响应: { access_token: "...", ... }
成功获取access_token，开始获取用户信息...
```

或者：
```
尝试方式1: Basic Auth
Basic Auth失败，尝试方式2: 参数在请求体
POST请求失败，尝试方式3: GET请求
Token响应: { access_token: "...", ... }
成功获取access_token，开始获取用户信息...
```

❌ **失败的日志**：
```
Token exchange failed: {
  status: 400,
  error: "error=invalid_request",
  ...
}
```

---

### 第4步：测试真实登录

1. **打开浏览器**，访问：https://aigctmp.wzbc.edu.cn

2. **按F12**打开开发者工具

3. **切换到Console标签**

4. **点击"登录"按钮**

5. **输入学号和密码**

6. **查看控制台输出**

✅ **成功的输出**：
```
开始调用Edge Function，参数: { code: "ST-...", state: "" }
Edge Function响应: { data: { success: true, userInfo: {...} }, error: null }
登录成功！正在跳转...
```

❌ **失败的输出**：
```
Edge Function错误: 获取access_token失败
```

---

## 🔍 调试技巧

### 实时查看日志

在一个终端窗口运行：
```bash
supabase functions logs oauth-callback --follow
```

然后在浏览器中测试登录，可以实时看到：
- 接收到的授权码
- 尝试的请求方式
- Token响应
- 用户信息
- 任何错误信息

### 查看网络请求

1. 按F12打开开发者工具
2. 切换到**Network**标签
3. 点击登录
4. 找到`oauth-callback`请求
5. 查看：
   - Request Headers
   - Request Payload
   - Response Headers
   - Response Body

---

## ✅ 验证清单

完成后，请确认：

- [ ] 执行了`./redeploy-edge-function.sh`
- [ ] Edge Function版本号增加了
- [ ] 测试脚本不再返回`invalid_request`
- [ ] 可以访问网站
- [ ] 可以点击登录并跳转到CAS
- [ ] 输入账号密码后能返回网站
- [ ] 浏览器控制台无500错误
- [ ] Edge Function日志显示成功获取access_token
- [ ] 登录成功，Header显示用户信息

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| **快速修复指南.md** | 快速参考和技术细节 |
| **OAuth请求修复记录.md** | 完整的修改历史（4次尝试） |
| **解决500错误.md** | 500错误的详细解决方案 |
| **修复401错误指南.md** | 401错误的解决方案 |

---

## 🆘 仍然失败？

如果执行上述步骤后仍然失败，请提供以下信息：

### 1. 重新部署输出
```bash
./redeploy-edge-function.sh 2>&1 | tee deploy.log
cat deploy.log
```

### 2. 测试结果
```bash
./check-supabase.sh 2>&1 | tee test.log
cat test.log
```

### 3. Edge Function日志
```bash
supabase functions logs oauth-callback --limit 50
```

### 4. 浏览器控制台输出
- 按F12 → Console标签
- 复制所有红色错误信息

### 5. 网络请求详情
- 按F12 → Network标签
- 找到`oauth-callback`请求
- 右键 → Copy → Copy as cURL

---

## 🎉 成功标志

当你看到以下内容时，说明修复成功：

1. ✅ 测试脚本返回200或400（不是500）
2. ✅ Edge Function日志显示"成功获取access_token"
3. ✅ 浏览器控制台显示"登录成功！正在跳转..."
4. ✅ Header显示用户信息（学号、姓名等）
5. ✅ 可以正常使用网站功能

---

**准备好了吗？开始执行第1步！** 🚀

```bash
cd ~/projects/app-7z58i603if41
./redeploy-edge-function.sh
```
